# DOCUMENTS THAT COMPILE AT A CERTAINTY LEVEL AND DO NOT MATCH THE ENGINE.
#
# The certainty levels promise agreement, so every line here is a broken promise, not an excuse.
# They are named rather than skipped so the gate still fails on anything NEW, and so the list can
# only shrink by fixing something. The o3 pipeline already demotes each of these to -O0, so the
# BEHAVIOUR is the engine's — what is wrong is the claim that -O1 needed no such demotion.
#
# One document per line: <Style>/<Name>  # reason

# THIS FILE IS EMPTY, and the gate still fails on anything new — which is the only state it was ever
# meant to reach.
#
# WHAT WAS HERE AND IS NOT ANY MORE. Judging -O1 over all five styles first found SEVEN documents,
# and six of them came down to three rules the compiler was missing rather than to six quirks:
#
#   * a binding that dereferences null must write NOTHING (JS throws; the engine keeps the default)
#   * QML's default for `property color` is OPAQUE BLACK, where a default-constructed QColor is
#     INVALID — different values that print differently
#   * a document's IMPORTS must be loaded, not only the module the document lives in, because a
#     module carries its resources
#
# Fusion's CheckIndicator, RadioIndicator, SliderGroove and ButtonPanel and Universal's
# CheckIndicator all agree with the engine now. Naming them here would have hidden three real gaps
# behind five well-documented excuses, which is the failure mode this file is one step away from.

# ...and the TWO DELAYBUTTONS, Basic's and Fusion's, which this file carried longest. The reading
# recorded here was right about the symptom and wrong about the cause. Completing in the engine's
# order — reverse creation order, which is what QQmlObjectCreator::finalize does with pop() — is
# NECESSARY and was never SUFFICIENT: on its own it cost four other documents, because it removed an
# accident that was hiding a second difference.
#
# The second one is Q_CLASSINFO("DeferredPropertyNames", "background,contentItem,indicator"). Qt does
# not build those children during the object pass at all; QQuickControl::componentComplete builds
# them, completes each as it is built, and resizes them only afterwards. We built them eagerly, so a
# control that had just been given its implicit height stretched a contentItem that had never laid
# out, and a QQuickText freezes baselineOffset at its FIRST layout.
#
# Both together: Basic 52 -> 53, Fusion 52 -> 53, Universal unchanged with its own DelayButton now
# agreeing, Imagine 41 -> 40, total 235 -> 236, and -O1 judging 110 documents instead of 108 with
# NOTHING left in this file. The one document it costs is Imagine's TextField, same family and same
# shape: data[0].baselineOffset 8.34375 where the engine has 5.34375 — the engine measured that Text
# at height 0 and we measured it at 6.
